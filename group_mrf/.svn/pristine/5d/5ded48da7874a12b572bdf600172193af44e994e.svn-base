# First, compute average mask file from all subjects' mask files.
combinemask --maskpath mymaskpath --out averagemaskfile.nii

# Second, remove all points where any fmri time series are zero. And apply threshold to get a binary mask.
cleanmask


initmarf

groupmrf



